{
  "name": "video-content-generator",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://content-generators.github.io/youtube-video",
  "dependencies": {
    "@content-generators/ui-components": "^1.0.1-PRE",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "copy_assets": "cp -R ./node_modules/@content-generators/ui-components/dist/public/ ./public/",
    "prestart": "npm run copy_assets",
    "start": "react-scripts start",
    "prebuild": "npm run copy_assets",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
